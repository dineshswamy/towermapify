
<%
	avail_op=[]
	avail_operators=Hash.new 
	@query.each do |l| 
	 if(avail_operators[l.provider_id]==nil)
   	avail_operators[l.provider_id]=getstyle(l.provider_id)
	 end
	 end
	 
	 avail_operators.each do |d|
	 	avail_op << d[0]
	 end
%>	
<%= @query.to_json(:only => [:city,:provider_id,:lat,:lon,:has_datacard,:has_hsia]) %>
